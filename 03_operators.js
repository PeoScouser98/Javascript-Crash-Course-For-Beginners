// toán tử trong Javascript
/**
 * toán tử gán
 * toán tử số học
 * toán tử so sánh
 * toán tử tăng giảm
 * toán tử logic
 */

// toán tử gán
const name = "hiệp ahihi";
let number = 1;
number += 1;
// number = number + 1;
// number = number - 1;
// number = number / 1;
// number = number * 1;
number = 1;
console.log(number);
// toán tử số học
// + - * /

// toán tử so sánh
/**
 *  <,> ,>=,<= ,== , === , !=
 *
 */
let x = 1;
let y = 1;
if (x == y)
	// so sánh  với 2 dấu = thì nó chỉ quan tâm đến giá trị chứ quan tâm đến kiểu dữ liệu
	console.log("ahihi");
if (x === y) console.log("ahihi");
else console.log("sai rồi");
// toán tử logic
let handsome = true;
let age = 17;
if (age > 18 || handsome == true) console.log("auto có người yêu");
// toán tử tăng giảm
// let number = 1;
// console.log(number--);
